# Docker container will have Python version: 3.12-slim.
# Check the Python image in the https://hub.docker.com/_/python
FROM python:3.12-slim

# Set working directory.
WORKDIR /home

COPY ./requirements.txt ./
COPY ./src ./src/
COPY ./assets ./assets/

RUN \
apt-get update && \
apt-get upgrade -y && \
apt-get autoremove -y && \
apt-get clean -y && \
pip install --upgrade pip && \
pip install wheel && \
pip install -r requirements.txt

# Expose the port the app runs on.
EXPOSE 8501

CMD ["streamlit", "run", "src/streamlit.py"]


# To build and run the Docker container, use the following commands:
# docker build -t streamlit_ac .
# docker run -p 8501:8501 streamlit_ac